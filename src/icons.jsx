const { Icon } = window.IconLibrary;

const makeIconComponent = (name) => ({ className, ...rest }) => (
  <Icon name={name} className={className} {...rest} />
);

// --- Dưới đây giữ nguyên danh sách icon cũ ---
const MapIcon = makeIconComponent('map');
const LayoutGrid = makeIconComponent('layout-grid');
const Settings = makeIconComponent('settings');
const Menu = makeIconComponent('menu');
const Target = makeIconComponent('target');
const Signal = makeIconComponent('signal');
const Play = makeIconComponent('play');
const Pause = makeIconComponent('pause');
const ArrowLeftRight = makeIconComponent('arrow-left-right');
const CornerUpLeft = makeIconComponent('corner-up-left');
const CornerUpRight = makeIconComponent('corner-up-right');
const MoreHorizontal = makeIconComponent('more-horizontal');
const Video = makeIconComponent('video');
const Ruler = makeIconComponent('ruler');
const Layers = makeIconComponent('layers');
const Save = makeIconComponent('save');
const Navigation = makeIconComponent('navigation');
const AlertTriangle = makeIconComponent('alert-triangle');
const LocateFixed = makeIconComponent('locate-fixed');
const Compass = makeIconComponent('compass');
const Tractor = makeIconComponent('tractor');
const Route = makeIconComponent('route');
const Activity = makeIconComponent('activity');
const Globe = makeIconComponent('globe');
const X = makeIconComponent('x');
const ChevronRight = makeIconComponent('chevron-right');
const Monitor = makeIconComponent('monitor');
const Cpu = makeIconComponent('cpu');
const Radio = makeIconComponent('radio');
const Plus = makeIconComponent('plus');
const Minus = makeIconComponent('minus');
const Sun = makeIconComponent('sun');
const Moon = makeIconComponent('moon');
const FolderOpen = makeIconComponent('folder-open');
const FileText = makeIconComponent('file-text');
const Trash2 = makeIconComponent('trash-2');
const CheckCircle2 = makeIconComponent('check-circle-2');
const Gauge = makeIconComponent('gauge');
const RotateCcw = makeIconComponent('rotate-ccw');
const RotateCw = makeIconComponent('rotate-cw');
const Flag = makeIconComponent('flag');
const Calendar = makeIconComponent('calendar');
const Sprout = makeIconComponent('sprout');
const Droplets = makeIconComponent('droplets');
const Scissors = makeIconComponent('scissors');
const GitCommitHorizontal = makeIconComponent('git-commit-horizontal');
const ArrowUpFromDot = makeIconComponent('arrow-up-from-dot');
const Spline = makeIconComponent('spline');
const CircleDashed = makeIconComponent('circle-dashed');
const MousePointer2 = makeIconComponent('mouse-pointer-2');
const Disc = makeIconComponent('disc');
const Crosshair = makeIconComponent('crosshair');
const Square = makeIconComponent('square');
const PlayCircle = makeIconComponent('play-circle');
const CheckSquare = makeIconComponent('check-square');
const MapPin = makeIconComponent('map-pin');
const Keyboard = makeIconComponent('keyboard');
const Check = makeIconComponent('check');
const ChevronDown = makeIconComponent('chevron-down');
const ChevronsRight = makeIconComponent('chevrons-right');
const PenTool = makeIconComponent('pen-tool');
const PlusCircle = makeIconComponent('plus-circle');
const AlertCircle = makeIconComponent('alert-circle');
const Copy = makeIconComponent('copy');
const AlignJustify = makeIconComponent('align-justify');
const Eye = makeIconComponent('eye');
const EyeOff = makeIconComponent('eye-off');
