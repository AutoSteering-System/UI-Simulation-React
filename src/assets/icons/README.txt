Drop SVG files here. The filename (without .svg) is the icon name used in code.
Example: src/assets/icons/map.svg -> <MapIcon /> or <Icon name="map" />
Notes:
- SVG should use stroke="currentColor" or fill="currentColor" to inherit Tailwind text color.
- Size follows Tailwind width/height classes on the icon component.
